{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @workspace/api-server run build && cd artifacts/haven-web && PORT=3000 BASE_PATH=/ node ../../node_modules/.bin/vite build --config vite.config.ts && ls dist/public/index.html && cp -r dist/public ../../_site",
  "outputDirectory": "_site",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "includeFiles": "artifacts/api-server/dist/**"
    }
  }
}
