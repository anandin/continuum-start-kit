{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd /vercel/path0 && pnpm --filter @workspace/api-server run build && cd /vercel/path0/artifacts/haven-web && PORT=3000 BASE_PATH=/ npx vite build --config vite.config.ts && cp -r dist/public /vercel/path0/_site",
  "outputDirectory": "_site",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "includeFiles": "artifacts/api-server/dist/**"
    }
  }
}
