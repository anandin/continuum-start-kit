{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "ROOT=$PWD && pnpm --filter @workspace/api-server run build && cd $ROOT/artifacts/haven-web && PORT=3000 BASE_PATH=/ npx vite build --config vite.config.ts && cp -r dist/public $ROOT/_site",
  "outputDirectory": "_site",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "includeFiles": "artifacts/api-server/dist/**"
    }
  }
}
